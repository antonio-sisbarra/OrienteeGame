<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:paddingBottom="@dimen/activity_vertical_margin"
              android:paddingLeft="@dimen/activity_horizontal_margin"
              android:paddingRight="@dimen/activity_horizontal_margin"
              android:paddingTop="@dimen/activity_vertical_margin"
              android:theme="@style/AppTheme">

    <!-- Layout che contiene un'icona e l'indicatore del punteggio-->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/activity_horizontal_margin"
        android:paddingEnd="@dimen/activity_horizontal_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:textSize="@dimen/font_size_text_hello_game"
        android:id="@+id/totalpointstext"
        android:textColor="@color/colorPrimary"
        android:fontFamily="@string/font_fontFamily_medium"
        android:text="Ciao Antonio, hai totalizzato 1000 punti"
        android:drawableStart="@android:drawable/ic_dialog_info"
        android:drawableLeft="@android:drawable/ic_dialog_info"
        android:drawablePadding="@dimen/activity_horizontal_margin"
        android:drawableTint="@color/colorPrimaryDark"
        android:layout_gravity="bottom"
        android:layout_weight="1"/>

    <!-- TextView che fa da titolo alle partite disponibili -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/activity_horizontal_margin"
        android:paddingEnd="@dimen/activity_horizontal_margin"
        android:textSize="@dimen/font_size_text_hello_game"
        android:textColor="@color/colorPrimary"
        android:fontFamily="@string/font_fontFamily_medium"
        android:text="@string/available_matches"
        android:drawableStart="@android:drawable/ic_dialog_map"
        android:drawableLeft="@android:drawable/ic_dialog_map"
        android:drawablePadding="@dimen/activity_horizontal_margin"
        android:drawableTint="@color/colorPrimaryDark"
        android:layout_gravity="bottom"
        android:layout_weight="1"/>

    <!-- TODO:ListView con le tipologie di partite disponibili -->
    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="50">


    </ListView>

</LinearLayout>
